{
  "name": "mirfa-secure-tx",
  "private": true,
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "pnpm -C packages/crypto test",
    "lint": "turbo lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}
